# Vercel — required for production (Railway backend URL, no trailing slash)
# Example: https://zomatomilestone-production.up.railway.app
VITE_API_URL=

# Local dev: leave empty — Vite proxies /api and /images to http://127.0.0.1:8000
